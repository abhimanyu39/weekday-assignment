import { Box } from "@mui/material";
import Card from "../JobCard/Card";

const Content = () => {
  return (
    <Box m={2} p={1} component={"main"}>
      <Card />
    </Box>
  );
};

export default Content;
